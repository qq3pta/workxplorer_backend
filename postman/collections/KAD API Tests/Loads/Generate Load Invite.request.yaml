$kind: http-request
name: Generate Load Invite
url: "{{base_url}}/api/loads/{{cargo_uuid}}/invite/generate/"
method: POST
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 4502741492592378
