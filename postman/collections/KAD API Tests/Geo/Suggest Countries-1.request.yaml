$kind: http-request
name: Suggest Countries
url: "{{base_url}}/api/geo/suggest/countries/?q={{country_query}}&limit=10"
method: GET
queryParams:
  q: "{{country_query}}"
  limit: "10"
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 1773312565685
