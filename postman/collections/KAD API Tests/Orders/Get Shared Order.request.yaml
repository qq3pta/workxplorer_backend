$kind: http-request
name: Get Shared Order
url: "{{base_url}}/api/orders/orders/shared/{{share_token}}/"
method: GET
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 4503599318357846
