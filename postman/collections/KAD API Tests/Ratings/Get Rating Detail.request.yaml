$kind: http-request
name: Get Rating Detail
url: "{{base_url}}/api/ratings/ratings/{{rating_id}}/"
method: GET
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 3893296059066991
