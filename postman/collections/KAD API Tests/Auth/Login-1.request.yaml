$kind: http-request
name: Login
url: "{{base_url}}/api/auth/login/"
method: POST
body:
  type: json
  content: |-
    {
        "email": "test@example.com",
        "login": "customer",
        "password": "1234",
        "remember_me": true
    }
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 1773311536630
