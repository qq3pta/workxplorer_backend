$kind: http-request
name: Create Consultation Request
url: "{{base_url}}/api/support/consultation/"
method: POST
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 4422936581646073
