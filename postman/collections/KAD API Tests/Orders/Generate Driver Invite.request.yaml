$kind: http-request
name: Generate Driver Invite
url: "{{base_url}}/api/orders/{{order_id}}/generate-invite/"
method: POST
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 4503491141903521
